version: "3"
services:
  app:
    build:
      context: .
    ports:
      - "9095"
  nginx:
    image: nginx:latest
    volumes:
      - /home/ubuntu/myproject/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    ports:
      - "8990:80"
